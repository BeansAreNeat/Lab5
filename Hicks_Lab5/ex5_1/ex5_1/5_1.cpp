#include "5_1.h"
#include <iostream>
using namespace std;

void linkedList::insertFront(int data)
{
	// Dynamically created node for each time function runs
	listNode* newNode = new listNode();
	// The new pointer, node, pointers to the data
	newNode->data;

	cout << "newNode = " << newNode << endl;
	cout << "Data = " << data << endl;
}